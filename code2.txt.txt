先用srand（）函数设置随机种子确保rand函数生成的数字随机，之后用二重循环生成矩阵，用同样的方法使数字随机
顺时针旋转，我觉得可以这样做。
假设随机生成的矩阵为A[m][n]
B[m][n]={{0}}
B[m][n]=A[m][n+1]  
当m=n+1时  //右上角
B[m][n]=A[m-1][n]
当m-1=1时  //右下角
B[m][n]=A[m][n-1]
当n-1=1时  //左下角
B[m][n]=A[m+1][n]
当m+1=2时 即A[2][1]时
把中间的（m-2）*（n-2）矩阵看做新的矩阵重复上述操作
最后把B[0][0]定义为最后剩下的数

